@page "/subjects"

<DataTable TData="SubjectDto" TParameters="SubjectParameters"
           ParameterOptions="@(parameters => parameters.OrderBy = "Name")"
           SearchTerms="@(new List<(string placeHolder, Expression<Func<SubjectParameters, string?>>)> { ("first name", parameters => parameters.Name)})"
           TableHeaders="@(new List<(string, string)>{("name", "Name")})"
           TableData="@(new List<Func<SubjectDto, string>> { s => s.Name })"
           CreateLink="@("/subjects/Create")"
           EditLink="/subjects/edit"/>