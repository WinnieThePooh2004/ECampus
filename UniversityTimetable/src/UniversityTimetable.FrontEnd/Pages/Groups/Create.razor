@page "/groups/create/{DepartmentId:int}"
@inherits CreatePageBase<GroupDto>

<GroupEditForm Model="new GroupDto {DepartmentId = DepartmentId}" OnSubmit="Save" />

@code{
    [Parameter] public int DepartmentId{ get; set; }
    protected override string PageAfterSave => $"/groups/{DepartmentId}";
}