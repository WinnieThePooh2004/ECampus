@page "/departments/{FacultyId:int}"

<h1>Departments</h1>
<DataTable TData="DepartmentDto" TParameters="DepartmentParameters"
                ParameterOptions="p => p.FacultacyId = FacultyId"
                TableHeaders="@(new(){ "Name" })"
                TableData="@(new(){ f => f.Name })"
                CreateLink="@($"/departments/Create/{FacultyId}")"
                EditLink="/departments/edit"
                ActionLinks="@(new(){ ("View teachers", d => $"/Teachers/{d.Id}"), ("View groups", d => $"/Groups/{d.Id}") })" />
