@page "/faculties/edit/{Id:int}"
@inherits EditPageBase<FacultyDto>
<h3>Edit</h3>
@if(Model is null)
{
    <p><em>Loading...</em></p>
    return;
}
<FacultyEditForm Model="Model" OnSubmit="@(async model => await Save(model))" />
<a role="button" class="btn-link" href="/faculties">Back</a>