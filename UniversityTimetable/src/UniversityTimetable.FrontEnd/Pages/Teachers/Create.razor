@page "/teachers/create/{DepartmentId:int}"
@inherits CreatePageBase<TeacherDTO>

<TeacherEditForm Model="new(){ DepartmentId = DepartmentId }" OnSubmited="Save" />

@code{
    [Parameter]
    public int DepartmentId { get; set; }

    protected override string PageAfterSave => $"/teachers/{DepartmentId}";
}