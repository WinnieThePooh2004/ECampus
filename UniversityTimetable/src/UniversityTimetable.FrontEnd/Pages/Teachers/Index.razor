@page "/teachers/{DepartmentId:int}"
<IndexPageModel TData="TeacherDTO" TParameters="TeacherParameters"
                ParameterOptions="p => p.DepartmentId = DepartmentId"
                TableHeaders="@(new(){ "Last name", "First name" })"
                TableData="@(new(){ t => t.LastName, t => t.FirstName })"
                CreateLink="@($"/teachers/Create/{DepartmentId}")"
                EditLink="/teachers/edit"
                ActionLinks="@(new(){ ("Timetable", d => $"/timetable/teacher/{d.Id}")})" />