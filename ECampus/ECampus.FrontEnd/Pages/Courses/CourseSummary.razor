@page "/courses/Summary/{StudentId:int}"

<IndexPageModel TData="ECampus.Domain.DataTransferObjects.CourseSummary" TParameters="CourseSummaryParameters" EditEnabled="false"
                ParameterOptions="SetParameters"/>

@code{

    [Parameter] public int StudentId { get; set; }

    private void SetParameters(CourseSummaryParameters parameters)
    {
        parameters.StudentId = StudentId;
        parameters.OrderBy = nameof(ECampus.Domain.DataTransferObjects.CourseSummary.CourseId);
    }
}