@page "/teachers/create/{DepartmentId:int}"
@using ECampus.Shared.DataTransferObjects
@inherits CreatePageBase<ECampus.Shared.DataTransferObjects.TeacherDto>

<TeacherEditForm Model="new TeacherDto{ DepartmentId = DepartmentId }" OnSubmit="Save" />

@code{
    [Parameter]
    public int DepartmentId { get; set; }

    protected override string PageAfterSave => $"/teachers/{DepartmentId}";
}