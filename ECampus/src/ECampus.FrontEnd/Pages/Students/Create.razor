@page "/students/create/{GroupId:int}"
@using ECampus.Shared.DataTransferObjects
@inherits CreatePageBase<ECampus.Shared.DataTransferObjects.StudentDto>

<StudentEditForm Model="new StudentDto {GroupId = GroupId}" OnSubmit="Save" />

@code{
    [Parameter] public int GroupId{ get; set; }
    protected override string PageAfterSave => $"/Students/{GroupId}";
}